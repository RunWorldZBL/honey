# Tauri Sidecar Binaries

This directory is reserved for generated Tauri sidecar executables.

`pnpm build:backend-sidecar` emits files like:

```text
honey-backend-x86_64-pc-windows-msvc.exe
```

The generated binaries are intentionally ignored by git. Keep this README tracked so the directory and naming convention remain visible.
